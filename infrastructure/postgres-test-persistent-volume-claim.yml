apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-test-data-disk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-premium
  resources:
    requests:
      storage: 5Gi
